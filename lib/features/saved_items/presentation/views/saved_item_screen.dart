import 'package:flutter/material.dart';

class SavedItemScreen extends StatelessWidget {
  const SavedItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Saved Item Screen"),
    );
  }
}
